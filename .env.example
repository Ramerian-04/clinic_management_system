# Copy this file to .env and fill in real values.
# .env itself is git-ignored — this example file IS meant to be committed,
# so teammates know which variables they need to set up locally.

SECRET_KEY=
DEBUG=True

DB_NAME=cms_db
DB_USER= #add your username  
DB_PASSWORD= #add your password
DB_HOST=localhost
DB_PORT=3306
